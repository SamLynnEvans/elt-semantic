name: "elt"
version: "1.0.0"
config-version: 2

profile: "elt"

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

models:
  elt:
    staging:
      +schema: staging
      materialized: table
    marts:
      +schema: marts
      +materialized: table